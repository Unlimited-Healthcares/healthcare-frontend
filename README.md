## Discovery Invitations Debugging

Set the following env vars for local debugging:

```
VITE_API_URL=https://api.unlimtedhealth.com/api
VITE_DEBUG_INVITES=true
```

These enable structured logs for `/invitations` and `/requests` with correlation IDs.

# healthcare-frontend
